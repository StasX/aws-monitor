replicas: 1

ingress:
  enabled: true

pod:
  # name: flask-aws-monitor
  # image: "sm1986/flask-aws-monitor"
  # tag: "latest"
  targetPort: 5001
  port: 80

awsSecretName: aws-credentials

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"

  limits:
    cpu: "500m"
    memory: "512Mi"