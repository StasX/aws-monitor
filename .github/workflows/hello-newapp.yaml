name: hello-newapp

on:
  push:
  workflow_dispatch:

jobs:
  deploy-app:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
      name: checkout
      with:
        sparse-checkout: |
          .github
          chart
