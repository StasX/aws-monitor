name: alert-workflow

on:
  workflow_run:
    workflows: ["main-workflow"]
    types:
      - completed
    branches:
      - "github-action"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: test
      run: |
        echo ${{ github.event.workflow_run.conclusion }}
        echo ${{ github.event.workflow_run.conclusion == 'failure' }}
  alert-failure:
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    runs-on: ubuntu-latest
    steps:
    - name: notify
      uses: slackapi/slack-github-action@v3.0.3
      with:
        webhook: ${{ secrets.SLACK_API}}
        webhook-type: incoming-webhook
        payload: |
          text: "Pipe line in workflow: ${{ github.event.workflow_run.name }} failed, please check the logs"
  alert-success:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
    - name: notify
      uses: slackapi/slack-github-action@v3.0.3
      with:
        webhook: ${{ secrets.SLACK_API}}
        webhook-type: incoming-webhook
        payload: |
          text: "Pipe line in workflow: ${{ github.event.workflow_run.name }} succeeded"